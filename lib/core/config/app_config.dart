class AppConfig {
  // Geocerca por defecto (coordenadas provistas)
  static const double defaultSchoolLat = 21.224842738543487;
  static const double defaultSchoolLon = -99.92340204636226;
  static const double defaultGeofenceRadiusM = 100.0; // 100 metros

  // Id de escuela por defecto mientras no hay selección
  static const String defaultSchoolId = 'default';
}
